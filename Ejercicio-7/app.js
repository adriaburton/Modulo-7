let objecte = ["taula", "cadira", "ordinador", "libreta"];
let posicio = ["0", "1", "2", "3"];
for (let index = 0; index < 4; index++) {
  console.log(
    "L´objecte " + objecte[index] + " esta a la posició " + posicio[index] + "."
  );
}
